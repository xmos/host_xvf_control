
DFU_CONTROLLER_SERVICER_RESID (0xF0):
  dedicated_commands:
    - cmd: DFU_DETACH
      index: 0
      type: CMD_WRITE_ONLY
      value_type: TYPE_UINT8
      number_of_values: 1
      help: "DFU Detach command. No valid payload."
      hidden: false
    - cmd: DFU_DNLOAD
      index: 1
      type: CMD_WRITE_ONLY
      value_type: TYPE_UINT8
      number_of_values: 61
      help: "DFU Download command. Length byte followed by 60 bytes of data."
      hidden: false
    - cmd: DFU_UPLOAD
      index: 2
      type: CMD_READ_ONLY
      value_type: TYPE_UINT8
      number_of_values: 61
      help: "DFU Upload command. Length byte followed by 60 bytes of data."
      hidden: false
    - cmd: DFU_GETSTATUS
      index: 3
      type: CMD_READ_ONLY
      value_type: TYPE_UINT8
      number_of_values: 5
      help: "DFU Get Status command. Status byte, 3 byte timeout, state byte."
      hidden: false
    - cmd: DFU_CLRSTATUS
      index: 4
      type: CMD_WRITE_ONLY
      value_type: TYPE_UINT8
      number_of_values: 1
      help: "DFU Clear Status command. No valid payload."
      hidden: false
    - cmd: DFU_GETSTATE
      index: 5
      type: CMD_READ_ONLY
      value_type: TYPE_UINT8
      number_of_values: 1
      help: "DFU Get State. Returns 1 byte state number."
      hidden: false
    - cmd: DFU_ABORT
      index: 6
      type: CMD_WRITE_ONLY
      value_type: TYPE_UINT8
      number_of_values: 1
      help: "DFU Abort command. No valid payload."
      hidden: false
    - cmd: DFU_SETALTERNATE
      index: 64
      type: CMD_WRITE_ONLY
      value_type: TYPE_UINT8
      number_of_values: 1
      help: "DFU additional command. Sets factory (0) or upgrade (1) DFU target"
      value_ranges:
        value0: [0 .. 1]
      hidden: false
    - cmd: DFU_TRANSFERBLOCK
      index: 65
      type: CMD_READ_WRITE
      value_type: TYPE_UINT8
      number_of_values: 2
      help: "DFU additional command. Sets the transfer block for upload. Internally autoincrements."
      default_value: 0
      hidden: false
    - cmd: DFU_VERSION
      index: 88
      type: CMD_READ_ONLY
      value_type: TYPE_UINT8
      number_of_values: 3
      help: "DFU Servicer-specific version command. Returns device version. Temporary for now, until a decision is made on command map integration into the DFU app."
      hidden: false
    - cmd: DFU_REBOOT
      index: 89
      type: CMD_WRITE_ONLY
      value_type: TYPE_UINT8
      number_of_values: 1
      help: "DFU Servicer-specific reboot command. No valid payload. Temporary for now, until a decision is made on command map integration into the DFU app."
      hidden: false
  shared_commands:
  external_commands:
DFU_RESID (0xF1):
  dedicated_commands:
  shared_commands:
  external_commands:
